import { AccountStatusTranslationPipe } from './account-status-translation.pipe';

describe('AccountStatusTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountStatusTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
